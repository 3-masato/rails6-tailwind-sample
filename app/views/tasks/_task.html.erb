<div class="flex items-center gap-2">
  <div class="w-96 py-2 px-3 leading-6 border border-transparent"><%= task.title %></div>
  <%= render(ButtonComponent.new(
    style: :outline,
    color: :blue,
    options: {
      path: edit_task_path(task),
      remote: true,
      class: "w-8 h-8"
    }
  )) do %>
    <i class="fa-solid fa-pencil"></i>
  <% end %>
</div>