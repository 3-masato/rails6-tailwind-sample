{
  const el = document.getElementById("<%= @task.hashed_id %>");
  el.innerHTML = "<%= j(render "tasks/edit", task: @task) %>";
  console.log("edit");

  const cancelButton = document.getElementById("cancel-<%= @task.hashed_id %>");
  function cancelEdit() {
    el.innerHTML = "<%= j(render "tasks/task", task: @task) %>";
  }
  cancelButton.addEventListener("click", cancelEdit, { once: true });
}